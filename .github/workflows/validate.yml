name: Validate proposed data
on:
  pull_request:
permissions:
  contents: read
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          python3 -m unittest discover -s scripts -p 'test_*.py'
          python3 scripts/build_site.py
